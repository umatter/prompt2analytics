%% =============================================================================
%% PERFORMANCE AND DEPLOYMENT
%% =============================================================================

\section{Performance and deployment} \label{sec:deployment}

The three principles require software fast enough to run locally and
models small enough to fit on consumer hardware. This section presents
performance benchmarks against \proglang{R} and describes fully local
deployment configurations where all components---including the language
model---run on the user's machine.

\subsection[Performance comparison with R]{Performance comparison with \proglang{R}}

Table~\ref{tab:benchmark-full} presents benchmark results from a unified
validation and benchmarking pipeline that simultaneously measures
correctness, execution time, and memory usage. Both \proglang{R} and
\proglang{Rust} benchmarks operate on identical datasets generated with
shared random seeds (seed~=~42) and matching data-generating processes;
timings measure computation only, excluding data loading and process
startup. To ensure clean apples-to-apples comparisons, all speedup
figures reported here use $n = 1{,}000$ observations (see
Appendix~\ref{app:benchmark-methodology} for the full pipeline covering
multiple sample sizes).
\proglang{R} benchmarks use canonical reference packages:
\fct{lm}/\pkg{sandwich}~\citep{Zeileis:2004} for regression,
\pkg{plm}~\citep{Croissant+Millo:2008}/\pkg{lfe}~\citep{Gaure:2013}
for panel data, \pkg{AER} for IV,
\pkg{forecast}~\citep{Hyndman+Khandakar:2008}/\pkg{vars} for time
series, and \proglang{R}'s \pkg{stats} for hypothesis tests.
Across 100 matched methods at $n = 1{,}000$, the median speedup is
12$\times$, with \proglang{Rust} faster in 89\% of cases
(Figure~\ref{fig:benchmark-histogram}).

Speedups vary widely by method category, reflecting the relative weight
of interpreter overhead versus numerical computation.
\emph{Panel data} methods show large gains from avoiding formula parsing
and factor handling: random effects (65$\times$), Hausman test
(53$\times$), fixed effects (22$\times$), and HDFE (20$\times$).
\emph{Time series} methods range widely: VAR (33$\times$), ARIMA
(8$\times$), MSTL (7$\times$), and STL (3$\times$).
\emph{Statistical tests} cluster around 5--15$\times$: $t$-tests
(15$\times$), Kolmogorov-Smirnov (9$\times$), and Shapiro-Wilk
(5$\times$). \emph{Regression} methods range from 3$\times$ (LOESS)
to 9$\times$ (OLS). \emph{Discrete choice} models achieve 3--10$\times$:
negative binomial (40$\times$), Poisson (4$\times$), logit (4$\times$),
and probit (3$\times$). \emph{Causal inference} gains include IV/2SLS
(8$\times$) and difference-in-differences (6$\times$).
\emph{Survival analysis} shows mixed results: log-rank (7$\times$) and
Kaplan-Meier (4$\times$), though Cox PH and AFT are comparable to
\proglang{R}'s optimized \pkg{survival} package.
\emph{Machine learning} methods show the most modest gains
(K-means 6$\times$, PCA 4$\times$, DBSCAN 2$\times$), reflecting the
competitiveness of \proglang{R}'s underlying \proglang{C} implementations
for these algorithms.

Complete benchmark results are in
Appendix~\ref{app:benchmark-methodology}; reproduction instructions
are in Appendix~\ref{app:reproducibility}.

% Table generated by: Rscript paper/code/tab_benchmark_summary.R
\input{tables/tab_benchmark_summary}

\begin{figure}[htbp]
\centering
\IfFileExists{figures/benchmark_histogram.pdf}{%
  \includegraphics[width=\textwidth]{figures/benchmark_histogram}%
}{%
  \fbox{\parbox{\textwidth}{\centering\vspace{3cm}%
    [Figure: benchmark\_histogram.pdf]\\%
    Generate with: \texttt{Rscript paper/code/generate\_paper\_figures.R}%
    \vspace{3cm}}}%
}
\caption{Distribution of Rust/R speedup factors across 100 matched methods
at $n = 1{,}000$ (log scale). The vertical line at
1$\times$ indicates parity; values to the right indicate \proglang{Rust}
faster. The median speedup is 12$\times$, with \proglang{Rust} faster
in 89\% of methods.}
\label{fig:benchmark-histogram}
\end{figure}

The performance advantages stem from \proglang{Rust}'s ownership system
(stack allocation and in-place mutation without garbage collection),
the \pkg{faer} library's SIMD-optimized linear algebra, the
column-based API that eliminates formula parsing, and the
single-language design that avoids data serialization between
components. \pkg{prompt2analytics} performs best on computationally intensive
iterative methods (panel fixed effects, treatment effect estimators,
causal inference, robust standard errors) where compile-time
optimizations and memory efficiency compound across iterations. For
machine learning methods dominated by optimized C code (PCA, K-means,
DBSCAN), \proglang{R}'s compiled implementations provide competitive
performance.

\paragraph{Memory efficiency.}
Beyond execution time, \proglang{Rust}'s deterministic memory
management reduces heap allocation. Across the 100 matched methods
at $n = 1{,}000$, \proglang{Rust} typically allocates 2--50$\times$
less memory than the equivalent \proglang{R} call (median
3$\times$). The
largest savings appear in panel data and treatment effect methods,
where \proglang{R}'s formula interface and factor expansion create
intermediate copies that \proglang{Rust}'s ownership model avoids.
Figure~\ref{fig:benchmark-memory} and Table~\ref{tab:memory-summary}
show the memory comparison by module.

\begin{figure}[htbp]
\centering
\IfFileExists{figures/benchmark_memory.pdf}{%
  \includegraphics[width=\textwidth]{figures/benchmark_memory}%
}{%
  \fbox{\parbox{\textwidth}{\centering\vspace{3cm}%
    [Figure: benchmark\_memory.pdf]\\%
    Generate with: \texttt{Rscript paper/code/generate\_paper\_figures.R}%
    \vspace{3cm}}}%
}
\caption{Memory allocation comparison between \proglang{R} and
\proglang{Rust} implementations by method category. \proglang{Rust}
consistently allocates less memory, with the largest savings in panel
data and treatment effect methods.}
\label{fig:benchmark-memory}
\end{figure}

% Table generated by: Rscript paper/code/generate_paper_tables.R
\input{tables/tab_memory_summary}

For users with CUDA-capable hardware, \pkg{p2a-core} provides optional
GPU acceleration via cuBLAS and cuSOLVER. The \code{cuda} feature flag
enables GPU code paths at compile time; at runtime, a singleton context
attempts CUDA initialization and falls back silently to CPU if no
device is available. Public function signatures are unchanged---users
do not modify their code.

Not all operations benefit from GPU offloading. Transfer overhead
between host and device memory dominates for small problems, and
bandwidth-bound operations (e.g., matrix-vector products) can be
faster on CPU even for large $n$. The dispatch logic therefore checks
two conditions before routing to GPU: a minimum problem dimension
(e.g., $k \geq 30$ columns for $\bm{X}'\bm{X}$) and a minimum
computational intensity (e.g., $nk^2 \geq 2 \times 10^7$). Both
thresholds are configurable via environment variables, allowing
users to tune dispatch for their specific hardware. A shape guard
additionally prevents GPU dispatch for tall-skinny matrix
multiplications, where the CPU path is substantially faster.

Table~\ref{tab:gpu-ops} summarizes the operations with GPU code paths
and observed speedups on an NVIDIA DGX~Spark (Grace Blackwell, 12~GB
VRAM, unified memory). The largest gains appear in $\bm{X}'\bm{X}$
computation (up to 7$\times$ for $k \geq 100$) and PCA via the
covariance path (up to 13$\times$ for tall matrices). K-means distance
computation and sandwich estimators for heteroskedasticity-robust
standard errors see more modest improvements of 2--3$\times$.

\begin{table}[htbp]
\centering
\begin{threeparttable}
\caption{GPU-accelerated operations and observed speedups}
\label{tab:gpu-ops}
\small
\begin{tabular}{lp{4.7cm}rl}
\toprule
Operation & Use in & Speedup & Condition \\
\midrule
$\bm{X}'\bm{X}$ (DGEMM) & OLS, panel FE/RE, IV, robust SEs & 2--7$\times$ & $k \geq 30$ \\
PCA covariance & Dimensionality reduction & 4--13$\times$ & $n > 4k$ \\
K-means distances & Clustering & 2--3$\times$ & $d \geq 20$ \\
Sandwich $\bm{X}'\bm{\Omega X}$ & HC0--HC3 robust SEs & 2--3$\times$ & $k \geq 30$ \\
\bottomrule
\end{tabular}
\begin{tablenotes}[flushleft]
\item \emph{Note:} Measured on NVIDIA DGX~Spark (Grace Blackwell, unified
memory). Speedups are relative to the CPU path on the same machine
(ARM~Cortex-X925 with OpenBLAS). Results will differ on discrete GPUs
where host--device transfer costs are higher.
\end{tablenotes}
\end{threeparttable}
\end{table}

Most econometric applications involve moderate numbers of regressors
($k < 30$), where CPU execution is faster. GPU acceleration is
therefore not part of the default build; it targets high-performance
computing scenarios with many covariates, high-dimensional clustering,
or large-scale bootstrap procedures. The implementation exploits a
layout convention---\pkg{ndarray}'s row-major storage appears as a
transposed column-major matrix to cuBLAS---that avoids data
reordering on the host side. Operations that are bandwidth-bound
rather than compute-bound ($\bm{X}'\bm{y}$ via DGEMV) remain
on CPU, where OpenBLAS is consistently faster. The feature is
currently limited to NVIDIA GPUs via CUDA; extension to other
backends (e.g., Vulkan compute, Metal) is left for future work.

\subsection{Fully local deployment} \label{sec:local}

While cloud-hosted language models offer convenience, many use cases
require complete data isolation. The fully local configuration runs all
components on the user's machine via Ollama~\citep{Ollama}, eliminating
all external network communication. Appendix~\ref{app:deployment}
provides platform-specific installation instructions.

The evaluation results (Section~\ref{sec:evaluation}) inform practical
model recommendations. Open-source models deployable via Ollama---such
as Ministral~3B (80.2\% adequate) and Llama~4~Scout (50.0\%)---enable
fully local analysis without cloud connectivity, and Ministral~3B's
80\% adequate rate shows that data isolation need not come at the cost
of tool selection quality for standard workflows.

Table~\ref{tab:models} summarizes model options with hardware
requirements and recommended use cases.  Cloud-hosted models
(GPT-4.1~Mini, Sonnet~4.6, Haiku~4.5, Gemini~2.5~Flash) provide
higher accuracy but require API access; the table focuses on locally
deployable options.

\begin{table}[htbp]
\centering
\begin{threeparttable}
\caption{Local deployment: models, hardware, and use cases}
\label{tab:models}
\begin{tabular}{lrrrrl}
\toprule
Model & Params & Storage & RAM & Adeq.\ (\%) & Recommended for \\
\midrule
\multicolumn{6}{l}{\textit{Evaluated models (locally deployable via Ollama)}} \\
Llama 4 Scout   & 17B$^a$ & 12 GB & 16 GB & 50.0 & Simple single-turn analyses \\
Ministral 3B    & 3B      & 2.0 GB & 6 GB  & 80.2 & General local use \\
\midrule
\multicolumn{6}{l}{\textit{Additional local options (not in our evaluation)}} \\
Llama 3.3 70B   & 70B  & 40 GB & 48 GB & ---$^b$ & Multi-turn workflows \\
Qwen 2.5 7B     & 7B   & 4.4 GB & 8 GB  & ---$^b$ & Balanced accuracy \\
\bottomrule
\end{tabular}
\begin{tablenotes}[flushleft]
\item \emph{Note:} Adequate rate from the 96-prompt end-to-end
evaluation (Section~\ref{sec:evaluation}). Storage refers to Q4\_K\_M
quantization (4-bit). RAM is memory during inference, typically
1.5--2$\times$ storage due to KV cache and computational buffers.
$^a$~17B active parameters, 109B total (mixture of experts).
$^b$~Not included in our evaluation; recommended based on external
tool-calling evaluations.
\end{tablenotes}
\end{threeparttable}
\end{table}

Ministral~3B demonstrates that even very small models achieve viable
accuracy.  With only 3~billion parameters, it runs on virtually any
modern laptop---approximately 6~GB VRAM for GPU inference, or CPU-only
with 8~GB RAM.  Its adequate rate (80.2\%) is within 4~percentage
points of Claude Haiku~4.5 (81.2\%), a cloud-hosted model.
The 7--8B parameter class offers a practical middle ground,
fitting on consumer GPUs with 8+ GB VRAM (e.g., NVIDIA RTX 3070/4070)
or Apple M-series devices with 16~GB unified memory.

Beyond LLM memory, users must budget RAM for dataset operations.
A typical configuration analyzing 100,000 observations with a 7B
model requires approximately 8--10~GB total RAM; a minimal laptop
setup (Ministral 3B, small datasets) works within 16~GB.
Appendix~\ref{app:hardware-requirements} provides detailed RAM
requirements by dataset size
(Table~\ref{tab:hardware-data}) and system configurations for
common deployment scenarios (Table~\ref{tab:hardware-summary}).

\subsection{Privacy guarantees}

The fully local configuration provides complete data isolation. No
network requests are made to external services. All model inference
occurs on the user's hardware, dataset contents never leave the
machine, and query history remains in local storage. This architecture
is suitable for analysis of sensitive data where regulatory requirements
(GDPR, HIPAA, or institutional review board protocols) or organizational
policy prohibit cloud processing. Users should verify that their Ollama
installation does not enable telemetry or automatic model updates.
Appendix~\ref{app:privacy} provides detailed data transmission
documentation and security best practices.
